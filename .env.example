# Cible du proxy serveur (dev + prod) — jamais exposé au navigateur
BACKEND_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3001
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=

# Optionnel : bypass proxy (cookies auth cassés entre ports/domaines)
# NEXT_PUBLIC_USE_DIRECT_API=true
# NEXT_PUBLIC_API_URL=http://localhost:3000

# Prod (Vercel)
# BACKEND_URL=https://tunrent-backend.onrender.com
# Ne pas définir NEXT_PUBLIC_API_URL en prod
